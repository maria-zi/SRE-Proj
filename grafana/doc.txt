# КАК ЗАПУСТИТЬ МОНИТОРИНГ
  # 1. Добавь метрики в приложение (обнови app/main.py и app/requirements.txt)

# 2. Установи зависимости
cd telecom-system/app
pip install -r requirements.txt

# 3. Запусти основное приложение
cd ..
docker-compose up -d

# 4. Запусти мониторинг
chmod +x scripts/start-monitoring.sh
./scripts/start-monitoring.sh

# 5. Открой Grafana
# http://localhost:3000
# Login: admin
# Password: telecompass

 # ГОТОВЫЕ ДАШБОРДЫ ДЛЯ GRAFANA:
#После запуска Grafana импортируй готовые дашборды (Dashboard → Import):

#Дашборд	ID	Что показывает
#Prometheus 24/7	8999	Метрики Prometheus, scrape status
#Node Exporter	1860	CPU, память, диск, сеть (хост)
#cAdvisor	193	Docker контейнеры (CPU, память, сеть)
#PostgreSQL	9628	DB connections, queries, slow queries
#FastAPI Custom	создать самому	API requests, latency, error rate

  #Создай алерты в Grafana (Alerting → New Alert Rule):

Алерт	            Условие                 	Severity
High CPU	       CPU > 80% > 5 мин	      Warning
High Memory	     Memory > 90% > 5 мин	     Critical
API Down	       API requests = 0 > 1 мин	   Critical
High Latency	   p95 latency > 2 сек	      Warning
Error Rate	     5xx errors > 5%	         Critical
DB Connections	 Connections > 90% max	   Warning
Disk Space	     Disk free < 10%	          Critical


# 3. Установи зависимости
cd telecom-system/app
pip install -r requirements.txt


# 6. Проверь метрики
curl http://localhost:8000/metrics

# Должно показать что-то вроде:
# # HELP api_requests_total Total API requests
# # TYPE api_requests_total counter
# api_requests_total{method="GET",endpoint="/health",status="200"} 5
# # HELP api_request_duration_seconds API request duration
# # TYPE api_request_duration_seconds histogram
# api_request_duration_seconds_bucket{method="GET",endpoint="/health",le="0.005"} 5
